---
title: 35 有监督微调 SFT
description: 指令微调、数据格式、训练流程
---

# 35 有监督微调 SFT

预训练后的 LLM 只会"续写文本"，不会"回答问题"。SFT（Supervised Fine-Tuning）教会模型**遵循指令**——这是从"语言模型"到"助手"的关键一步。

## 为什么需要 SFT

预训练模型的典型表现：

```text
输入: "什么是机器学习？"
预训练模型输出: "机器学习是人工智能的一个分支。什么是深度学习？深度学习是机器学习的子集。什么是..."
```

它在**续写**，而不是**回答**。SFT 的目标：

```text
输入: "什么是机器学习？"
SFT 后输出: "机器学习是人工智能的一个分支，它使计算机能够从数据中学习规律，而无需显式编程。"
```

## 指令微调 vs 传统微调

| | 传统微调 | 指令微调（SFT） |
|---|---------|----------------|
| 目标 | 特定任务（分类、NER） | 通用指令跟随 |
| 数据 | 任务标注数据 | 指令-回答对 |
| 效果 | 一个模型一个任务 | 一个模型多种能力 |
| 代表 | BERT 微调 | ChatGPT、LLaMA-Chat |

## SFT 数据格式

### ChatML 格式

```text
<|im_start|>system
你是一个有用的AI助手。<|im_end|>
<|im_start|>user
什么是机器学习？<|im_end|>
<|im_start|>assistant
机器学习是人工智能的一个分支...<|im_end|>
```

### 数据结构

```python
# 典型的 SFT 数据格式
{
    "instruction": "请解释什么是机器学习",
    "input": "",
    "output": "机器学习是人工智能的一个分支，它使计算机能够从数据中自动学习规律..."
}

# 对话格式
{
    "conversations": [
        {"role": "user", "content": "什么是机器学习？"},
        {"role": "assistant", "content": "机器学习是..."}
    ]
}
```

### 构建训练样本

```python
def build_sft_sample(tokenizer, instruction, response, max_length=2048):
    """构建 SFT 训练样本"""
    messages = [
        {"role": "user", "content": instruction},
        {"role": "assistant", "content": response},
    ]

    # 用 tokenizer 的 apply_chat_template 构建
    full_text = tokenizer.apply_chat_template(messages, tokenize=False)

    # 编码
    full_ids = tokenizer.encode(full_text, max_length=max_length, truncation=True)

    # 构建 labels：只在 assistant 回答部分计算 loss
    user_text = tokenizer.apply_chat_template(
        [{"role": "user", "content": instruction}],
        tokenize=False,
        add_generation_prompt=True,
    )
    user_len = len(tokenizer.encode(user_text))
    labels = [-100] * user_len + full_ids[user_len:]

    return {"input_ids": full_ids, "labels": labels}
```

**关键**：`labels` 中 user 部分设为 `-100`（PyTorch 会忽略），只在 assistant 回答部分计算 loss。

## 数据质量 > 数量

LIMA 论文（Zhou et al., 2023）用**仅 1000 条高质量数据**微调 LLaMA-65B，效果接近 GPT-4。

**关键发现**：
- 数据质量远比数量重要
- 多样性比数量重要
- 1000 条精选数据 > 100K 条低质量数据

**数据质量检查清单**：
- 回答是否准确？
- 回答是否完整？
- 格式是否规范？
- 是否有多样性（不同任务类型）？

## SFT 训练流程

```python
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer

# 加载模型
model_name = "Qwen/Qwen2.5-1.5B"
model = AutoModelForCausalLM.from_pretrained(model_name, torch_dtype="auto")
tokenizer = AutoTokenizer.from_pretrained(model_name)

# LoRA 配置（高效微调）
lora_config = LoraConfig(
    r=8,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj"],
    lora_dropout=0.05,
    task_type="CAUSAL_LM",
)
model = get_peft_model(model, lora_config)

# 训练参数
training_args = TrainingArguments(
    output_dir="output/sft",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=8,
    learning_rate=2e-5,
    warmup_ratio=0.1,
    lr_scheduler_type="cosine",
    bf16=True,
    logging_steps=10,
    save_strategy="epoch",
)

# 训练
trainer = SFTTrainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset,
    tokenizer=tokenizer,
    max_seq_length=2048,
)
trainer.train()
```

## 常见问题

### 过拟合

- **症状**：训练 loss 很低，但生成质量下降
- **解决**：减少 epoch（1-3 轮通常够）、增加数据多样性、用 LoRA 限制参数量

### 灾难性遗忘

- **症状**：SFT 后模型丧失了预训练学到的知识
- **解决**：混入预训练数据（5-10%）、降低学习率、用 LoRA（只更新少量参数）

### 回答格式不一致

- **症状**：有时回答很长、有时很短
- **解决**：数据中的回答格式要统一，加入长度和格式约束

## 评估方法

| 评估方式 | 说明 |
|---------|------|
| 人工评估 | 最准确，但成本高 |
| GPT-4 评估 | 用 GPT-4 打分（MT-Bench 风格） |
| 自动指标 | BLEU、ROUGE（粗糙但快） |
| 基准测试 | MMLU、C-Eval、HumanEval |
| 指令跟随率 | IFEval 评测格式遵守能力 |

## 小结

| 概念 | 说明 |
|------|------|
| SFT | 有监督微调，教会模型遵循指令 |
| ChatML | 标准对话格式（system/user/assistant） |
| Labels Masking | user 部分 loss=-100，只在回答部分训练 |
| 数据质量 | 1000 条高质量 > 100K 条低质量 |
| 灾难性遗忘 | 混入预训练数据、用 LoRA 缓解 |

## 进一步阅读

1. **Zhou et al., 2023.** *LIMA: Less Is More for Alignment.* [arXiv:2305.11206](https://arxiv.org/abs/2305.11206)
2. **Ouyang et al., 2022.** *Training language models to follow instructions with human feedback.* [arXiv:2203.02155](https://arxiv.org/abs/2203.02155) — InstructGPT
3. **Taori et al., 2023.** *Stanford Alpaca.* [GitHub](https://github.com/tatsu-lab/stanford_alpaca)
